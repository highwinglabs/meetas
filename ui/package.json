{
  "name": "meeting-assistant-ui",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "description": "Lokale React-Oberfläche für den Meeting-Assistent Core (Phase 4). Läuft als Client der REST-API auf 127.0.0.1; vom Core statisch ausgeliefert.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "i18n:check": "node scripts/check-i18n.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
